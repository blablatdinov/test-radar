# SPDX-FileCopyrightText: Copyright (c) 2025-2026 Almaz Ilaletdinov <a.ilaletdinov@yandex.ru>
# SPDX-License-Identifier: MIT

services:

  test-radar-http:
    image: blablatdinov/test-radar:${TAG_NAME}
    command: .venv/bin/gunicorn server.wsgi:application -b 0.0.0.0:8012
    restart: always
    ports:
      - 8012:8012
    env_file:
      - .env
    environment:
      - APP_VERSION=${TAG_NAME}


# docker build . -t blablatdinov/test-radar:v0.1.0 && TAG_NAME=v0.1.0 docker compose -f deploy/docker-compose.prod.yml up -d
